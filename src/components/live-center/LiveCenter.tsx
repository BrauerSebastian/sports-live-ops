import type { EventStatus, Incident } from "@/app/page";

export function LiveCenter({ status, homeScore, awayScore, minute, incidents, commentary }: { status: EventStatus; homeScore: number; awayScore: number; minute: number; incidents: Incident[]; commentary: string[] }) {
  return <div className="public-view"><header className="public-header"><div className="public-wordmark">SPORTS LIVE OPS <span>/ LIVE CENTER</span></div><span className="public-competition">North American League / Matchday 08</span><button className="follow-button">Follow match</button></header><main className="public-main"><div className="public-intro"><div><p className="overline">Northeast Division / Round 08</p><h1>Northbridge FC <span>vs</span> Eastvale United</h1><p className="event-location">Riverside Stadium</p></div><span className="public-live-status"><span className="state-light" />{status === "Live" ? "Live now" : status}</span></div><section className="public-score"><div className="public-team"><span className="public-badge">N</span><strong>Northbridge FC</strong></div><div><strong>{homeScore} - {awayScore}</strong><time>{status === "Live" ? `${minute}:23` : status}</time></div><div className="public-team"><span className="public-badge east-badge">E</span><strong>Eastvale United</strong></div></section><div className="public-tabs"><button className="active">Timeline</button><button>Commentary</button><button>Statistics</button></div><div className="public-content"><section><div className="public-section-title"><h2>Match timeline</h2><span>Updated live</span></div><div className="public-feed">{incidents.slice().reverse().map((incident, index) => <div className="public-event" key={`${incident.minute}-${index}`}><time>{incident.minute}&apos;</time><span className={`public-event-marker ${incident.type}`} /><div><strong>{incident.label}</strong><p>{incident.team ?? "Match official"}{incident.detail ? ` / ${incident.detail}` : ""}</p></div></div>)}</div></section><aside className="public-commentary"><div className="public-section-title"><h2>Commentary</h2><span>From the touchline</span></div>{commentary.map((item, index) => <div className="public-note" key={`${item}-${index}`}><time>{index === 0 ? `${minute}'` : `${minute - 2}'`}</time><p>{item}</p></div>)}</aside></div></main><footer className="public-footer">Live information supplied by Sports Live Ops / Fictional competition data</footer></div>;
}

